# News Server Configuration

# Cron schedule (when to download news)
# Format: minute hour day-of-month month day-of-week
# Default is daily at midnight
CRON_TIME="0 0 * * *"

# GitHub repository to keep pulling new or updated recipes
# If empty, won't pull from git
GITHUB_REPO_URL=""

# How to handle duplicates when adding the same news again
# (overwrite, new_record, or ignore)
DUPLICATE_STRATEGY="new_record"

# Calibre credentials
CALIBRE_USER=admin
CALIBRE_PASSWORD=your_secure_password_here

# Container library folder (where calibre stores the library)
LIBRARY_FOLDER="/opt/library"

# Container recipes folder
RECIPES_FOLDER="/opt/recipes"

# Container user database path
USER_DB="/opt/users.sqlite"

# Log directory
LOG_DIR="/var/log/news_server"

# Timezone (use your local timezone to have consistent timestamps)
TZ=UTC
